# OSV-Scanner ignore config (also consumed by OpenSSF Scorecard's
# "Vulnerabilities" check, which reports advisories found in this repo's
# dependency manifests via https://osv.dev).
#
# See https://github.com/google/osv-scanner#ignore-vulnerabilities-by-id for
# the file format.

[[IgnoredVulns]]
id = "GHSA-4j2p-28q2-5m79"
reason = """
accelerate<=1.14.0 (transitive dependency via docling-slim, pinned in
requirements-ci.txt) has an open path traversal / DoS advisory (also tracked
as CVE-2026-69112) in load_checkpoint_in_model / load_checkpoint_and_dispatch,
which fail to sanitize weight_map entries from sharded checkpoint indexes.
1.14.0 is the latest release on PyPI; no patched version exists yet.
Semantica does not call either function or load arbitrary/untrusted sharded
checkpoints, so the vulnerable code path is not reachable. Re-evaluate once
accelerate ships a fix - see .github/workflows/security-scan.yml for the
matching pip-audit exclusion.
"""

# ---------------------------------------------------------------------------
# uv.lock is a universal lock over EVERY optional extra, so it necessarily
# includes the opt-in `crewai` extra. crewai==1.6.1 hard-pins
# `chromadb~=1.1.0` and `json-repair~=0.25.2`; the entries below are reachable
# only through `pip install semantica[crewai]` (or `uv sync --extra crewai`).
# The extra is deliberately excluded from `all` (see pyproject.toml), so it is
# not in requirements-ci.txt and never installed by CI, releases, or a plain
# `uv sync`. Semantica itself never imports chromadb or json_repair.
# Re-evaluate once crewai relaxes these pins / ChromaDB ships a fix.
# ---------------------------------------------------------------------------

[[IgnoredVulns]]
id = "GHSA-f4j7-r4q5-qw2c"
reason = """
chromadb 1.1.1 (via the opt-in crewai extra only; CVE-2026-45829): pre-auth
code injection in the ChromaDB *server* collections API. No fixed release
exists. Semantica does not run a ChromaDB server.
"""

[[IgnoredVulns]]
id = "GHSA-36p7-vc44-83pf"
reason = """
chromadb 1.1.1 (via the opt-in crewai extra only; CVE-2026-45833): authenticated
code injection in the ChromaDB *server* collections API. No fixed release
exists. Semantica does not run a ChromaDB server.
"""

[[IgnoredVulns]]
id = "GHSA-xph7-9rjv-w5fr"
reason = """
chromadb 1.1.1 (via the opt-in crewai extra only; CVE-2026-45831):
SimpleRBACAuthorizationProvider ignores tenant/database/collection scope in the
ChromaDB *server*. No fixed release exists. Semantica does not run a ChromaDB
server or use its authorization providers.
"""

[[IgnoredVulns]]
id = "GHSA-2wm9-hf6c-p5cr"
reason = """
chromadb 1.1.1 (via the opt-in crewai extra only; CVE-2026-45830): missing
tenant authorization checks in the ChromaDB *server*. No fixed release exists.
Semantica does not run a ChromaDB server.
"""

[[IgnoredVulns]]
id = "GHSA-xf7x-x43h-rpqh"
reason = """
json-repair 0.25.2 (via the opt-in crewai extra only): unbounded CPU use when
json_repair's SchemaRepairer follows a circular JSON Schema $ref. The fix
(0.60.1) is blocked by crewai's json-repair~=0.25.2 pin. Semantica does not
import json_repair or feed it untrusted schemas.
"""
